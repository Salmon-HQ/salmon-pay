import { listPayments,refreshHumanPayment } from '@/lib/payment-service';
export async function POST(request:Request){const secret=process.env.CRON_SECRET;if(!secret||request.headers.get('authorization')!==`Bearer ${secret}`)return Response.json({error:'unauthorized'},{status:401});const pending=(await listPayments()).filter(payment=>payment.status==='pending').slice(0,100),results=await Promise.allSettled(pending.map(refreshHumanPayment));return Response.json({data:{processed:results.length,checked:results.filter(result=>result.status==='fulfilled').length,failed:results.filter(result=>result.status==='rejected').length}})}
