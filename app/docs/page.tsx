import Link from 'next/link';
const rows=[['POST','/api/auth/bootstrap','Issue the first local dashboard key'],['POST','/api/payment-requests','Create a shared payment obligation · API key'],['GET','/api/payment-requests/:id','Public status + RPC settlement check'],['GET','/pay/:id','Canonical x402 v2 paid resource'],['GET','/api/x402/:id','Backward-compatible x402 alias'],['GET','/api/x402/status','Facilitator capability health'],['GET','/api/receipts/:id','Fetch settlement receipt'],['POST','/api/webhooks','Register a signed event endpoint · API key'],['POST','/api/webhooks/test','Send a signed test event · API key'],['GET','/api/webhooks/deliveries','Delivery and retry history · API key'],['POST','/api/api-keys','Issue a scoped key · API key'],['GET','/api/proxy/:id','Guard or proxy a paid resource']];
export default function Docs(){return <main className="docs-page"><Link href="/">← Salmon Pay</Link><h1>API reference</h1><p>Version 0 contract. JSON responses use a top-level <code>data</code> or <code>error</code> envelope. Mainnet x402 endpoints are exposed only when a compatible facilitator is configured.</p><div className="docs-list">{rows.map(([method,path,desc])=><div className="docs-row" key={path}><b>{method}</b><code>{path}</code><span>{desc}</span></div>)}</div></main>}
