export function parseUsdcAmount(input:unknown){const raw=String(input??'').trim().replace(/\s/g,'');if(!/^\d+(?:[.,]\d{1,6})?$/.test(raw))throw new Error('amount must use one decimal separator and at most 6 decimals');const [wholePart,fractionPart='']=raw.replace(',','.').split('.');const whole=BigInt(wholePart),fraction=fractionPart.padEnd(6,'0');const atomic=whole*1_000_000n+BigInt(fraction||'0');if(atomic<=0n)throw new Error('amount must be positive');let displayFraction=fractionPart.padEnd(2,'0');while(displayFraction.length>2&&displayFraction.endsWith('0'))displayFraction=displayFraction.slice(0,-1);return {display:`${whole}.${displayFraction}`,atomic:atomic.toString()}}
